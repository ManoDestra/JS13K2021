class Slogan extends Urge.Sprite {
	#text;

	constructor(context, text) {
		super(context);
		this.#text = text;
	}

	update(instant) {
		super.update(instant);
	}

	render(instant) {
		super.render(instant);
	}
}
